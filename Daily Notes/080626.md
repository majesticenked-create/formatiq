# Daily Note — 08/06/26

## Audit — T2 — lib/tools/registry.ts
- Result: **FAIL**
- Findings:
  - Regression: `base64-encoder-decoder`, `uuid-generator`, `word-counter` — `longDescription` still under ~40-word minimum (31, 29, 33 words). Flagged in a prior audit cycle, not fixed since.
  - `json-formatter` — the 4th tool from the same original batch — **is now fixed** (50 words), confirming this was addressable but the other 3 were missed.
  - Consistency warning (non-blocking): 6 tools missing the site-standard "runs client-side / no data leaves your browser" phrasing in `metaDescription`: `percentage-calculator`, `unit-converter`, `random-number-generator`, `whitespace-remover`, `age-calculator`, `bmi-calculator`.
  - No duplicate slug/category pairs; no invalid category values; `metaTitle` format consistent across all 51 tools.
  - All 51 tools have populated `useCase`, `metaTitle`, `metaDescription`, `keywords`, and >=2 FAQs — no empty fields.
- Actions:
  - Auditor logged a medium-severity incident to `.claude/logs/incident-log.md` (unresolved word-count regression across 2 audit cycles).
  - Auditor promoted a Hard Rule to `.claude/knowledge-base.md`: `longDescription` must be >=40 words for every `tools[]` entry.
  - Auditor updated its own memory with a Regression Watch List for these 3 slugs.
  - **Corrective task (open):** expand `longDescription` for `base64-encoder-decoder`, `uuid-generator`, `word-counter` to >=40 words — style/depth to match `json-to-csv`/`hash-generator`.
  - **Corrective task (open, lower priority):** add the standard client-side/no-upload phrasing to the 6 `metaDescription`s listed above.

## Corrective follow-up — same day
- Both corrective tasks closed same-session, per user confirmation:
  - `base64-encoder-decoder`, `uuid-generator`, `word-counter` `longDescription`s rewritten to 117/130/130 words respectively, matching the depth/style of `json-to-csv`/`hash-generator`.
  - All 6 flagged `metaDescription`s now end with "No data leaves your browser." for consistency with the other 45 tools.
  - `npm run build` re-confirmed clean (72 pages) after both fixes.
- Regression risk noted by auditor (word-count checks evaded 2 audit cycles) still stands as a process gap — no automated word-count check has been added yet. Recommend picking this up as a follow-up if it recurs a third time.

## Audit — T2 — lib/tools/registry.ts + components/tools/ (follow-up)
- Result: **WARN** (2 findings, both now fixed same day)
- Note: delegated auditor agent stalled mid-analysis 3 times in a row without producing a final report; findings were completed directly instead of via a 4th delegation attempt.
- Findings:
  - FAQ duplication: `json-formatter`, `json-to-csv`, and `url-encoder-decoder` all reused a near-identical FAQ question/answer template ("Is my [X] data uploaded anywhere? No. This tool runs entirely in your browser using JavaScript...") with only the noun swapped.
  - Error handling gap: `RandomNumberGenerator.tsx`'s Min/Max number inputs called `Number(e.target.value)` with no NaN guard (unlike the adjacent Count field, which had `|| 1`). An empty/invalid intermediate value would silently propagate to `NaN`, making `Array.from({length: NaN})` produce an empty result with no error message — a silent no-op.
  - Everything else re-verified clean: all 51 tools still >=40 words on `longDescription` (re-checked directly, zero under threshold), all 6 metaDescription patches from the earlier audit confirmed holding, all `JSON.parse`/`Number()`/`parseInt` call sites elsewhere in `components/tools/` confirmed guarded by prior regex/range validation.
- Actions:
  - Rewrote all 3 FAQ answers with genuinely tool-specific framing: `json-formatter` now mentions pasting API responses/config files, `json-to-csv` mentions spreadsheet exports of customer/order data, `url-encoder-decoder` mentions query strings carrying session tokens/API keys.
  - Added `|| 0` NaN guard to `RandomNumberGenerator.tsx`'s Min and Max inputs, matching the pattern already used on Count. Existing `min > max` / `count > rangeSize` checks already surface a clear inline message, so no further change was needed there once NaN was eliminated at the source.
  - `npm run build` confirmed clean (72 pages) after both fixes.
- Knowledge nomination (not yet promoted, needs a second occurrence per house process): "FAQ entries should not reuse the same question/answer template verbatim across tools, even when the underlying fact is shared."
